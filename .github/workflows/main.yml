name: Podcast generator

on:
  push:
  pull_request:
  workflow_dispatch:

jobs:
  terraform-validate:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      # Run feed generator from custom GitHub Action
      - name: Run feed generator
        uses: sverma092/podcast-generator@main
